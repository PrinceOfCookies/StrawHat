import type { APITeamChannel, CHANNEL_TYPES } from '@guildedjs/guilded-api-typings';
import { TeamChannel } from '../Channel';
import type { Client } from '../Client';
import type { Group } from '../Group';
import { BaseManager } from './BaseManager';
export declare class TeamGroupChannelManager extends BaseManager<APITeamChannel, TeamChannel> {
    group: Group;
    constructor(client: Client, group: Group);
    /**
     * Create a channel in the group this manager belongs to.
     */
    create({ name, parent, type, isPublic, }: {
        name: string;
        parent?: string;
        type: CHANNEL_TYPES;
        isPublic: boolean;
    }): Promise<unknown>;
}
//# sourceMappingURL=TeamGroupChannelManager.d.ts.map