import type { APIUser } from '@guildedjs/guilded-api-typings';
import type { Client } from '../Client';
import { User } from '../User';
import { BaseManager } from './BaseManager';
export declare class UserManager extends BaseManager<APIUser, User> {
    constructor(client: Client);
    /**
     * Fetch a user, retrieves from the cache if exists
     */
    fetch(id: string, cache?: boolean, force?: boolean): Promise<User>;
}
//# sourceMappingURL=UserManager.d.ts.map